---
import { Image } from "astro:assets";
import camp from "../assets/pics/camp.webp";
import hackathon from "../assets/pics/hackathon.webp";
---

<style>
  @reference "../styles/global.css";

  .section {
    @apply px-4 py-16;
  }
  .section-inner {
    @apply mx-auto max-w-6xl;
  }
  .grid-2col {
    @apply grid items-center justify-center gap-8 md:grid-cols-2;
  }
  .text-card {
    @apply flex flex-col items-start justify-center gap-4 rounded-2xl bg-white p-8 shadow-sm;
  }
  .img-wrapper {
    @apply relative aspect-5/2 overflow-hidden rounded-2xl shadow-sm;
  }
  .img-overlay {
    @apply absolute inset-0 flex items-end bg-linear-to-t from-black/60 to-transparent p-6;
  }
  .img-overlay-title {
    @apply text-2xl font-bold text-white;
  }
</style>

<main class="bg-background text-foreground">
  <section class="section">
    <div class="section-inner">
      <div class="grid-2col">
        <div class="img-wrapper group">
          <Image
            src={camp}
            alt="營隊活動 - SITCON 夏日學院學生實作工作坊"
            class="h-full w-full object-cover"
          />
        </div>

        <div class="text-card">
          <h2 class="text-primary-blue text-3xl font-bold">SITCON 夏日學院</h2>
          <p class="text-foreground">
            自 2014 年創立，SITCON Camp
            今年邁入第十屆。五天四夜的資訊旅程，以「探索型」與「導覽型」為核心，跳脫傳統入門框架——聚焦於已具備基礎、卻不知道下一步該往哪走的你。資安、AI、競程、系統、開源，每條路都值得被認真看見。
          </p>
          <p class="text-foreground">
            透過主題日
            Masterclass、社群闖關、視界咖啡館，由各領域實務專家親自帶路，在一天內真正觸碰資訊科學最迷人的那一面。這裡的學習不是單向灌輸，而是和隊輔、講者、夥伴一起討論、實作、卡關再突破——找到讓自己想持續鑽研的
            What's Next。
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="section-inner">
      <div class="grid-2col">
        <div class="text-card">
          <h2 class="text-primary-blue text-3xl font-bold">黑客松</h2>
          <p class="text-foreground">
            黑客松是 SITCON Camp
            的核心活動，也是整個營期的高潮。學員以小隊為單位，在有限時間內將五天所學轉化為真實可運行的作品，親身體驗「想法化為程式」的過程。不是為了比賽，而是為了真的做出一件事。
          </p>
          <p class="text-foreground">
            不論你擅長寫程式、做設計、規劃產品或負責溝通協作，每個人都能在隊伍中找到自己的位置。最終成果展示不只是終點，更是探索資訊領域下一步的起點——你會帶著一個真實作品離開這裡。
          </p>
        </div>

        <div class="img-wrapper group">
          <Image
            src={hackathon}
            alt="黑客松大賽 - 團隊協作coding實況"
            class="h-full w-full object-cover"
          />
        </div>
      </div>
    </div>
  </section>
</main>
